name: Validate Main

on:
  push:
    branches: [main]
  merge_group:

jobs:
  validate-main:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Sleep for 2 minutes
        run: sleep 120
